/**
 * Canonical page content container. Every page's content wraps in this so its
 * left/right edges line up with the site header and footer on the same gutter.
 * Universal: no product specifics — just the shared max-width + horizontal padding.
 */
export const PAGE_CONTAINER = 'mx-auto w-full max-w-[1280px] px-[26px]';
